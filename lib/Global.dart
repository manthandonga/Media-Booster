class Detail {
  String fullName;
  String email;
  String password;

  Detail({
    required this.fullName,
    required this.email,
    required this.password,
  });
}

Detail detail = Detail(fullName: "", email: "", password: "");
